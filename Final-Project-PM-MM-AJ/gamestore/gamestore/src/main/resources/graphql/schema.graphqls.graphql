type Query {
  consoles: [Console!]!
  consoleById(id: ID!): Console
  consoleByManufacturer(manufacturer: String!): [Console!]!
}

type Console {
  id: ID!
  model: String!
  manufacturer: String!
  memoryAmount: String
  processor: String
  price: BigDecimal!
  quantity: Int!
}